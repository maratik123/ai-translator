# Harness gaps

Diagnoses about the harness itself: what a flow failed to make happen, and where the fix belongs. The second learning log — conduct corrections go to [`learnings.md`](learnings.md), and the boundary between the two is stated in `AGENTS.md` § *Learning Log*.

An entry names the file its fix must land in, so a later reader can route it without re-deriving the diagnosis. The closing fields are written by a forge branch only, and `ai-docs/scripts/check-harness-gaps-forge.sh` enforces that.

The skeleton, fenced so the guards read it as a shape rather than as an entry:

```
### YYYY-MM-DD — one-line title
**target:** `path/to/file.md` § Section
**Observed:** what happened, with the coordinate it was observed at.
**Gap:** what the harness did not make happen.
**Proposed edit:** the change that would have made it happen.
**at:** <commit>
**Forge:** forge-N            (written by a forge branch only)
**Closed by:** #N             (written by a forge branch only)
**Superseded by:** YYYY-MM-DD (when a later entry replaces this one)
```

This log starts empty.

### 2026-09-18 — the interview-window hook decides on an instruction file's name in the COMMAND TEXT, not on the write's target
**target:** `.claude/settings.json` § the `PreToolUse` `Bash` hook that enforces Learning Log Boundary rule 2
**Observed:** during `/task` on issue #10, two Bash commands were refused with `BLOCKED: write into an instruction file while an interview is live`. Neither wrote to an instruction file. The first wrote owner answers into `ai-docs/plans/2026-09-18-workspace-crate-skeletons.spec.md.state.md`; the second, run by the `spec-writer` delegate, replaced the AC4 row inside the spec. The hook's guard is a conjunction of two greps over the command STRING — a path pattern (`\.claude/|AGENTS\.md|CLAUDE\.md|ai-docs/(code-style|doc-convention)\.md`) and a write pattern that includes `open\([^)]*["'][wa]["']`. Both commands satisfied the path half only because the TEXT BEING WRITTEN quoted an instruction file by name: an owner's question in the first case, the AC4 clause being struck in the second. Both satisfied the write half with a Python heredoc. Two independent agents hit it on the same shape within one run.
**Gap:** the hook judges the command's text rather than the file the command writes, so a legitimate write is refused whenever its CONTENT names an instruction file. That content is not rare in this flow — it is the flow's normal subject matter: a `SPEC-REMIT` finding quotes the standing rule it is about, and the answer, the spec row and the design row that resolve it all carry the quotation. The block message lists three legal routes and none of them is the one both agents actually took, so each had to infer that a dedicated edit tool is untouched by a `Bash` matcher.
**Proposed edit:** evaluate the path pattern against the write TARGET rather than the whole command — the redirect operand, the `sed -i` / `tee` operand, the first argument of `open()` — so that quoting a rule is never mistaken for editing it. If that is judged too fragile to parse, add the dedicated-edit-tool route to the block message's list, since the `Edit`/`Write` half of the same rule already keys on `file_path` and refuses the real case correctly.
**at:** 0b2bc15
**Forge:** forge-1

### 2026-09-18 — `/task` asserts a `.gitignore` state this repository does not have, and three steps are written against it
**target:** `.claude/skills/task/SKILL.md` § Step 8 and § Step 12 sub-step 9a (and `ai-docs/templates/progress-format.md` § Lifecycle by field, which repeats the claim)
**Observed:** Step 8 says the in-flight marker `ai-docs/plans/.task-inflight` is "(gitignored; Stop-hook contract)" and that the progress file needs `git add -f` "because the path matches a `.gitignore` glob; once tracked the glob no longer applies". Neither holds here. `.gitignore` has ten lines and no `ai-docs/` entry at all: `git check-ignore` reports `ai-docs/plans/.task-inflight`, `ai-docs/plans/<name>.progress.md` and `ai-docs/plans/ignored/<f>` all **not ignored**, while `tmp/<f>` is ignored. So the marker sits in `git status --porcelain` for the whole run, a plain `git add` tracks the progress file, and `ai-docs/plans/ignored/` becomes an untracked directory the moment Step 12 sub-step 9a moves the state files into it.
**Gap:** three instructions are written against the absent entries. Step 8's per-group cleanliness check offers "an empty `git status --porcelain`" as an equivalent of `git diff --quiet && git diff --cached --quiet`, and the two are not equivalent here — the marker the same step creates makes the first form non-empty for every handoff. Step 12 sub-step 9a then requires `git status --porcelain` empty **after** the move, which the untracked `ignored/` directory and the still-present marker both defeat. Neither failure is a real defect in the work, so the flow would be teaching its operator to read past its own verification.
**Proposed edit:** either add the entries the instructions assume (`ai-docs/plans/.task-inflight`, `ai-docs/plans/*.progress.md`, `ai-docs/plans/ignored/`) and keep the text, or drop the ignore claims and state the checks in the form that is actually sound here — `git diff --quiet && git diff --cached --quiet` for a handoff, and a status check scoped with `--untracked-files=no` or to the paths the step owns. The `git add -f` at Step 8 is harmless either way and can stay.
**at:** 5fd1a04
**Closed by:** #71

### 2026-09-19 — `/improve`'s clean-context eval cannot reach the probe-discipline or command-text classes
**target:** `ai-docs/improve-eval-contract.md` § The RED baseline
**Observed:** an `/improve` pass over eight correction entries escalated six of them as two proposals, assembled four reproducers and dispatched eight clean-context agents. All eight passed — four pre-change baselines, two post-change verifications and two load-bearing variants — so neither proposal ever obtained the FAIL-before the gate requires. Meanwhile the property under test recurred three times inside the same run: both agents dispatched on the command-text reproducer reported their naive first spelling matching their own shell and reporting a running process against an empty machine, and the orchestrating thread reproduced it when a control string it wrote put the target literal back into the command line a bracket class was protecting.
**Gap:** the instrument cannot separate *the rule is unnecessary* from *the rule's failure mode does not occur while the rule is the whole question*. Both classes are caught reliably by an idle agent asked about them directly and missed while doing something else — including under the load-bearing variant, whose primary task evidently did not demand enough attention to displace the clause.
**Proposed edit:** record these two classes as known-unreachable on the contract page, and state what a load-bearing variant must cost the dispatched agent before its PASS counts as evidence of reach rather than evidence of an undemanding primary task.
**at:** 136b290
**Forge:** forge-1

### 2026-09-19 — the documented `comment-refs` gated set is narrower than the coded one, and its `.githooks/**` claim is not implemented
**target:** `AGENTS.md` § Code Style and `ai-docs/doc-convention.md` § DOC-4 — a propagation pair, both carrying the list
**Observed:** both name the gated set as `*.rs`, `*.sh`, `*.sql`, `*.yml`, `*.yaml`, `.gitignore`, `Makefile` and everything under `.githooks/`. Selection is `git ls-files` filtered by `language_of`, which also gates `.bash` and `*.mk`, and which selects under `.githooks/` by extension alone — so the extensionless, symlinked `.githooks/pre-commit` is never scanned. The `.githooks` string near the top of the gate's module belongs to the repository-top-directory set the *token* classifier consumes, not to file selection, which is what a plain grep for it suggests.
**Gap:** latent today, since no tracked `.bash` or `.mk` file exists — but an extensionless hook added under `.githooks/`, which is git's ordinary spelling, would be silently ungated while both documents state it is covered.
**Proposed edit:** bring the two prose lists into agreement with the scanner's own language map, and replace the `.githooks/**` directory claim with the extension rule the code implements.
**at:** 136b290
**Forge:** forge-1

### 2026-09-19 — `/improve` Step 5 prescribes an English commit subject, which the language split forbids
**target:** `.claude/agents/self-improve.md` § Step 5 item 2b
**Observed:** the Commit B template is given literally as `chore(learnings): backfill Escalated? / Superseded by: for entries <date1>, <date2>, ...`. A run that followed it produced an English commit subject whose own body was Russian; self-review raised it as a major against `AGENTS.md` CRITICALLY 1 — *commit messages and pull-request bodies* sit on the Russian surface — and the subject was amended before the push.
**Gap:** the contract instructs the violation, so a run following Step 5 faithfully reproduces it. Nothing catches it except a review standing between the commit and the push, and after a push the only repair is a force-push, which § *Permissions* denies.
**Proposed edit:** give the Commit B template in Russian, or drop the literal subject and name the fields the message must carry plus a pointer to the language split.
**at:** 53adf8c
**Forge:** forge-1

### 2026-09-19 — `files_touched` keeps one path per bullet, admits duplicates, and no gate reads the section it parses
**target:** `.claude/skills/task/scripts/append-task-run.sh` § the `## Files touched` extractor
**Observed:** the only row in `ai-docs/metrics/task-runs.jsonl` carries 17 entries, 16 of them distinct, for a window `git diff --name-only 5fd1a04..3e14799` reports as 24 files. Two separate defects produce that. The extractor is `sed -nE 's/^-[[:space:]]+`([^`]+)`.*$/\1/p'`, which keeps the FIRST backticked token of a bullet and lets `.*$` swallow the rest, so the four bullets of the 2026-09-18 run that name two paths each (`` - `crates/shared/Cargo.toml`, `crates/shared/src/lib.rs` (new) ``) contributed one path each and every `.rs` file the task created is absent from the record. Separately, `ai-docs/claude-tools-hierarchy.md` appears twice, from two bullets recording two edits in two sections, so the array is a multiset of edit events rather than a set of files while the field table calls it `array<string>` and settles neither reading. The remaining four missing paths are bullets never written at all — `ai-docs/context-status.md`, `ai-docs/harness-gaps.md`, `ai-docs/learnings.md` and the progress file itself — and `self-review` DID find that half: progress `## Review register` row `R1-5`, `nit`, `accepted@1 — bookkeeping only`.
**Gap:** `## Files touched` is the sole source of a telemetry field and nothing says so on any page a reviewer reads, so a shortfall in it is classified as prose bookkeeping and closed below the severity floor. The section's shape is a convention (`ai-docs/templates/progress-format.md` § the template body) with no gate and no assertion; the extractor documents the one-path-per-bullet assumption in a comment and does not verify it.
**Proposed edit:** harvest every backticked path in a bullet's leading comma-separated chain and collapse repeats, so the value is the set of paths the section names; add a precondition assertion at Step 12 sub-step 5a that diffs the section against `git diff --name-only <base>..HEAD` with the extractor's own pipeline and is re-run until empty; pin all three shapes in `test-append-task-run.sh` with a registry row beside them.
**at:** 56aa372
**Forge:** forge-2

### 2026-09-19 — the diff-size trio is measured from a base that excludes the whole planning phase, and the schema page says the opposite
**target:** `ai-docs/task-run-schema.md` § *The diff-size trio — base commit and parsing*
**Observed:** the page justified its two-source base rule with "In the normal `/task` flow the two coincide — the branch is cut at Step 1 and the progress file created at Step 8 with no intervening commits". The flow contradicts that by construction: `**base_commit:**` is the creator's `git rev-parse HEAD` at Step 8 (`ai-docs/templates/progress-format.md` § *Lifecycle by field*), `/interview` commits the spec, its state file and the `INDEX.md` row before that, and `.claude/skills/task/SKILL.md` Step 6 orders the design committed "as soon as it lands, and after every design round". On the run of 2026-09-18 (issue #10) `**base_commit:** 5fd1a04` was the 13th of the branch's 28 commits; the record reads `24 / 401 / 38` where `git diff --shortstat ae76456..3e14799` over the whole branch at the same HEAD reads `28 / 1116 / 39`.
**Gap:** the script path measures implementation only and the fallback path measures the whole branch, so the two produce incomparable trios for the same run, and the page told every consumer they were the same measurement. No field names the base; `incomplete` is the only discriminator and nothing said to read it that way.
**Proposed edit:** state what the two bases actually measure, name `incomplete` as the discriminator, and record the measured 2026-09-18 figures as the evidence. A `base` field was considered and left out of v1 on the same grounds as the rest of § *What this log does NOT measure*.
**at:** 56aa372
**Forge:** forge-2

### 2026-09-19 — the severity floor routes findings into a section the telemetry parser is bounded away from
**target:** `ai-docs/task-run-schema.md` § *What this log does NOT measure*
**Observed:** the 2026-09-18 self-review (issue #10) returned APPROVE on round 1 having raised, verified and dispositioned ten findings — `R1-1` through `R1-10` in the progress file's `## Review register`, each `accepted@1` with its own verifying command. The record for that run reads `findings: {blocker:0, major:0, minor:0, nit:0}` and `findings_first_seen` likewise. Both halves are correct on their own terms: § *Section bounding* deliberately bounds parsing to the text between a `## Self-Review (Round N)` heading and the next `## `, and `ai-docs/templates/progress-format.md` § *`## Review register` semantics* deliberately defines `accepted@<round>` as "the durable form of *Recorded, not raised*".
**Gap:** the page calls `findings` "a *review-effort* measure (how much finding-handling the loop did)" and on this loop it is a count of table rows, which is not the same quantity. A run that handled ten sub-floor findings is byte-identical to one that handled none, and the page's list of ten things the log cannot see does not include this one — so a consumer has no warning at all.
**Proposed edit:** add the case to § *What this log does NOT measure* in that section's own shape (an undecided question with its consequence stated), and point at it from the `findings` counting-unit bullet so a reader of the field meets it.
**at:** 56aa372
**Forge:** forge-2

### 2026-09-19 — the `--no-verify` guard matches command text across `&&` and its block message does not say so
**target:** `.claude/settings.json` § the `PreToolUse` `Bash` hook that refuses `--no-verify`
**Observed:** during `/task` on issue #10 the command `git commit -q -m "…" && git push -q && echo pushed && grep -n '^\*\*current_step:' <progress-file>` was refused with `BLOCKED: --no-verify on git commit/push`. Neither flag was present; the matcher is `git[[:space:]]+(commit|push)\b[^;]*([[:space:]]--no-verify\b|[[:space:]]-[A-Za-z]*n[A-Za-z]*([[:space:]]|$))` and `[^;]*` stops only at a semicolon, so the scan ran past two `&&` to the trailing `grep -n`. The sibling piped-gate hook meets the same property and states it in its own refusal — "This hook matches COMMAND TEXT, not shell semantics" — plus a named false positive; this one states neither, and its three listed exits are all about coverage.
**Gap:** the breadth is the right trade and stays — narrowing the scan to stop at `&&` or `|` would let a pipe character inside a commit message carry `--no-verify` past the hook, which is the silent-and-expensive direction — but the agent that meets the block has to re-derive that from the regex. `ai-docs/scripts/test-no-verify-guard.sh` already pins two deliberate false positives in its header and this third shape was not among them.
**Proposed edit:** state the scan's extent and why it is not narrowed in the block message itself, and pin the observed shape as a third documented BLOCK fixture so a later narrowing fails in the suite rather than in production.
**at:** 56aa372
**Forge:** forge-2

### 2026-09-19 — the schema page's derived-membership figures are the source harness's, in the paragraph that forbids exactly that
**target:** `ai-docs/task-run-schema.md` § *`instruction_corpus_lines` — the pinned command*
**Observed:** the paragraph reads "Measured after the last edit of the commit that pins it (the figures this paragraph carried before were the source harness's, transcribed with the import — the copy this rule forbids): the counted set is 9,368 lines post-exclusion, so the 1 % threshold is 93.68 lines", and lists `ai-docs/harness-gaps.md` (93), `ai-docs/context-status.md` (45), `ai-docs/panic-index.md` (9) and `ai-docs/learnings.md` (76). `git log -- ai-docs/task-run-schema.md` returns exactly one commit, `bd2a33d`, the harness transplant. Measured at `bd2a33d` with the page's own pinned command: the counted set is 9,874 lines, and the four files are 21, 7, 13 and 5 lines. Not one figure has ever been true of this repository. The live consequence is the sentence "`harness-gaps.md` is within one line of the threshold: its next entry crosses it" — at `56aa372` the counted set is 10,016 lines, the threshold is 100.16, and `harness-gaps.md` is 61.
**Gap:** the rule the paragraph states is mechanical ("when a file that satisfies the criterion crosses 1 %, it **is** excluded at the next pinning") and has no detector, so a crossing can only be noticed by someone re-reading the paragraph — and the paragraph's numbers were wrong from the import, which is the state a re-reader would have trusted. The field itself is sound: `instruction_corpus_lines: 9957` reproduces exactly at `43ef5e9`.
**Proposed edit:** re-derive the figures at the commit that pins them, say plainly that the previous ones were never re-derived here, and add a crossing probe to § *Step-12 verification block* that prints each candidate file's share of the counted set every run, marking a crossing — reporting, never blocking, since exclusion happens at the next pinning.
**at:** 56aa372
**Forge:** forge-2

### 2026-09-19 — the inbox parser dropped prose with a stdout warning that has no durable destination
**target:** `.claude/skills/task/inbox-propagation.md` § *Unrecognised shape*
**Observed:** Step 12 sub-step 5 of the 2026-09-18 run (issue #10) emitted nine rows and one warning against the design's `## Open questions` section, whose bullets parsed and whose prose did not. The prose held a harness consequence with a wide radius — the comment-reference gate exempts a crate by DIRECTORY name while putting both directory and package into its name set, so a crate's own contract symbol is unwritable in the package-name form inside its own crate. It reached `ai-docs/key-decisions.md` KD-19 as a constraint and it reached the owner in the run's closing report, because the orchestrator carried the warning there by hand. It never became an inbox row or an issue.
**Gap:** the six shape rules were not total over a non-blank body, and the page's own answer for the remainder was a line on stdout — a surface that exists only while the session does, inside the one flow whose purpose is to make what a plan deferred findable later. The rule also specified the warning per SECTION while telling a mixed-shape body to classify per LINE, so what happens to a line matching nothing was each run's decision.
**Proposed edit:** add a seventh rule making the set total — a non-blank line matching none of rules 1–6 and not a continuation is an item of its own, its wrapped lines joined, with a `NOTE:` naming the shape — and state the fail direction as over-emission, since a row `/triage` dismisses is cheaper than an item nobody can find.
**at:** 56aa372
**Forge:** forge-2

### 2026-09-19 — Step 12's inbox parser is prose only; every run implements the seven rules again
**target:** `.claude/skills/task/inbox-propagation.md`
**Observed:** `.claude/skills/task/scripts/` holds `append-task-run.sh` and its suite, and nothing else. Sub-step 5's parser exists only as the rules on this page, so the 2026-09-18 run (issue #10) wrote its own at `tmp/inbox.py` and ran it once. Its warning text was not the page's — the page specified `unrecognised body shape, no rows emitted`, the run emitted `prose outside the bullet shapes, not emitted as rows` — and its handling of a mixed-shape body (rows for the bullets, a warning for the prose) was a reading the page did not state.
**Gap:** the single writer of `ai-docs/deferred/_inbox.jsonl` that `AGENTS.md` § *Workflow* names as an AXIOM is re-implemented per run from prose, with no fixture and no suite, so every rule on this page is a disposition rather than a protocol and drift between runs is invisible. The rule-7 edit of forge-2 narrows what can be decided ad hoc; it does not remove the re-implementation.
**Proposed edit:** ship the parser as `ai-docs/scripts/` or `.claude/skills/task/scripts/` code with a paired `test-*.sh` pinning each shape rule and the dedupe set's cardinality, and reduce this page to the contract the script implements. Left open deliberately: the artefact and its suite are their own change, and a forge that also authored them would be shipping a new single writer inside a patch about six other things.
**at:** 56aa372

### 2026-09-19 — `doc-edit-guard.sh` has no intended-shrink path, so an owner-ordered row removal cannot be guarded
**target:** `ai-docs/scripts/doc-edit-guard.sh` § verify
**Observed:** during `/task` on issue #13 the owner ruled that three acceptance rows be struck from the spec under interview. The `spec-writer` delegate skipped the guard for that edit and substituted a hand-built before/after diff, naming the omission in its return. Reproduced on a copy rather than taken on trust: `snapshot`, remove one `| AC` row, `verify` → exit 2, `ac-rows shrank 5 -> 4`, and the file restored from the backup. A growth control on the same copy exits 0 with `shape held`, so the guard is not simply red in every direction. `--help` lists `snapshot` and `verify`, and the case statement accepts no third form.
**Gap:** the guard reads every shrink as truncation — right for the failure it was built for, wrong for a deliberate removal the owner ordered. A delegate told to strike a row has only two exits: run the guard and have the ruling silently undone, or skip the guard for that edit. The second leaves the edit unguarded at the one moment the document is being reshaped, which is when the guard is worth most. The restore message also names a cause that is not the one in play — `the edit truncated the document (anchor matched an in-text mention?)` — sending a reader after an anchoring bug that does not exist.
**Proposed edit:** give `verify` a way to declare an intended shrink — an expected count, or an `--allow-shrink <marker>=<n>` argument recorded at snapshot time — so a deliberate removal stays guarded against every OTHER marker moving. Failing that, say in the usage text that a deliberate row removal is outside the guard's remit, and name the substitute check a delegate is expected to run in its place.
**at:** fe41d84

### 2026-09-19 — the relative-link check cannot tell a link from a quoted example of one, and its only escape is one hard-coded file name
**target:** `.github/workflows/ci.yml` § the `relative markdown links resolve` step
**Observed:** during `/task` on issue #13, a Group B delegate wrote a progress-file line quoting the inline-link form literally inside backticks, as prose about the form. The step reported `broken relative link … -> target` and exited 1 on a commit that introduced no link at all; the delegate reworded the sentence and the next commit was green, so the branch head never carried it. Reproduced here rather than taken from the report: the step's regex `\[[^\]]*\]\(([^)#\s]+)(#[^)]*)?\)` runs over `f.read_text()` with no awareness of backticks or fences, and against four constructed cases it flags a real broken link (correct), the identical form inside backticks (false), and the identical form inside a fenced code block (false), while a control string carrying no link form yields zero matches. The step already carries a hard-coded `or t == "file.md"` escape at line 270 — one placeholder name exempted by hand, which is the same class of false positive patched for a single instance rather than as a class.
**Gap:** a check meant to keep relative links honest also forbids writing *about* relative links, in a repository whose harness pages exist largely to describe their own conventions. The failure is silent in the worst direction for a reader: the error names a target that no author ever intended as a link, so the reported coordinate points at prose that is correct, and the cheapest repair is to reword the prose rather than to notice the instrument. Nothing warns an author that a fenced example will be scanned, and the single-name escape makes the behaviour look intentional for everything except that one name.
**Proposed edit:** strip fenced code blocks and inline code spans before the scan — the same two-step a markdown link checker normally takes — and delete the `file.md` special case once the class is handled, so the exemption list does not grow one placeholder at a time. If stripping is judged too fragile to do with a regex, say in the step's own comment that quoted link forms must be written without the parenthesis pair, so an author learns it from the instruction rather than from a red run.
**at:** 036de9a

### 2026-09-19 — the `grep` an agent invokes here is ugrep behind a shell function, and no search rule says so
**target:** `.claude/rules/ast-index.md` § Mandatory Search Rules
**Observed:** during `/task` on issue #13 a `self-review` delegate reported that its credential sweep "came back clean because it cannot match anything", naming the cause as ugrep silently failing a four-way alternation. The orchestrator checked the environment rather than the conclusion. Verified: `type -a grep` reports `grep — это функция` resolving to `ugrep 7.8.4`, while `/bin/grep --version` is `GNU grep 3.12` — so every `grep` an agent writes in a Bash call runs ugrep, and the GNU binary is reachable only by absolute path. **Not verified, and stated here so it is not carried forward as fact:** the specific failure the delegate diagnosed did not reproduce — a constructed line `password=hunter2` matched the same four-way alternation under ugrep, under the same pattern one scheme at a time, and under `/bin/grep`, all returning 1. The delegate's symptom (an empty sweep) was real; its causal explanation is unconfirmed, and the true cause of that particular empty result was not established.
**Gap:** the search rules name `grep` and `rg` as the fallbacks when the index does not cover a surface, and the probe discipline requires a control for every pattern — but nothing tells an agent that `grep` is not the tool its flags and edge-case behaviour are usually reasoned about. An agent writing a sweep reasons about GNU semantics, invokes something else, and the divergence surfaces only where the two differ, which is exactly the long-tail of complex patterns a credential or secret sweep lives in. The control requirement is what caught this one, and it is the only thing standing between the substitution and a false clean result.
**Proposed edit:** state in the search rules which binary `grep` resolves to in this environment and that `/bin/grep` is the GNU one, so an agent can choose deliberately; and note that a pattern whose emptiness is load-bearing should be run in the simple per-alternative form the same section already prefers over one long alternation. Re-measure the resolution rather than trusting this entry after a toolchain change — it is a property of the shell's profile, not of the repository.
**at:** 54debb3

### 2026-09-19 — the test-instrument rule names two failure modes and not the one this run produced three times
**target:** `AGENTS.md` § *Patterns* 2 — "When the instrument is a TEST"
**Observed:** that paragraph says "Two failure modes recur and both look like coverage — a fixture configured where the two branches coincide, and a test that hand-builds the object under test instead of going through the wiring that constructs it." A third shape appeared three separate times inside one `/task` run on issue #13, each past a green gate set and past a design-review GO: a start counter initialised to its expected value and never incremented, so the trial compared a constant with itself; a distinctness check run over the names the harness itself had handed back rather than over the server's answer, so a harness connecting elsewhere while reporting correct names would have passed; and a predicate asserting every migration's version is at least the minimum version, which is the definition of the minimum. None is a coinciding fixture and none hand-builds its subject — each goes through the real wiring and asserts something that cannot come out false. Swept for eight spellings of the shape (`cannot fail`, `always true`, `true by definition`, `no reachable failure`, `cannot report false`, `vacuous`, `trivially true`, `dead assertion`) across `AGENTS.md`, `ai-docs/rust-test-conventions.md`, `ai-docs/code-style.md` and `.claude/rules/ast-index.md`: zero files match any of them, against a constructed control file that matches all eight.
**Gap:** the paragraph's remedy — point the assertion at the broken mechanism and see it RED — does catch this shape, but only for someone who already suspects it. Both named modes are about the *setup* around an assertion, so a reader checking their work against the list examines fixtures and construction paths and finds them sound; the assertion's own logic is not what the list sends them to look at. All three instances here were caught by a reviewer reading the predicate, never by the gates, and the panic gate, the lint gate and the coverage ratchet are each blind to the shape by construction. The coverage ratchet is worse than blind: a dead assertion raises the line count, so the mode is rewarded by the one number that moves.
**Proposed edit:** add the third mode to the same sentence — an assertion whose predicate is true of every input the program could produce, named with its tell, that the quantity on both sides of the comparison comes from the same source. The remedy is already stated and needs no change; what is missing is the shape that sends a reader to apply it.
**at:** 763b1b7
