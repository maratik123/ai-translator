# Interview state — Postgres test harness and the vector-extension migration

Handoff between rounds, and the re-entry point for every later return to `spec-writer`. Kept on `ready`.

```yaml
schema_version: 1
spec_path: ai-docs/plans/2026-09-19-postgres-test-harness-migration.spec.md
issue_ref: "#13"
gh_issue:
  title: "Тестовая обвязка Postgres и первая миграция: расширение vector"
  state: open
  labels: ["infra", "storage"]
  issue_body_status: current
  body: |
    Первая задача вехи, которая касается БД. Она намеренно **не знает о схеме**: чтобы доказать себя, обвязке нужно живое соединение и что-нибудь, что можно спросить у базы, — но не девять таблиц.
    
    ## Объём
    - `testcontainers` с образом `pgvector/pgvector:pg18` через Podman socket (`DOCKER_HOST=unix:///run/user/$UID/podman/podman.sock`).
    - Версия образа совпадает с локальной машиной: Postgres 18.6 + pgvector 0.8.6.
    - Ryuk включён; контейнер один на тестовый бинарь (`OnceCell`).
    - **Миграция 1** — ровно одна инструкция: `CREATE EXTENSION IF NOT EXISTS vector`. Дока требует расширение именно первой миграцией.
    - `#[sqlx::test]` создаёт базу на тест и накатывает миграции сам (`docs/03-storage.md` § Задачи); на этой задаче накатывать нечего, кроме миграции 1.
    
    ## DoD
    - Тест поднимает контейнер, получает соединение, и `SELECT 'x'::vector` (или эквивалент) проходит — то есть образ действительно несёт pgvector, а не только называется так.
    - Машина без контейнерного рантайма узнаёт об этом из **падающего теста**, а не из тихого пропуска. Это надо увидеть красным, а не предположить: зелёная обвязка до первого наблюдавшегося падения — утверждение об обвязке, а не о машине.
    - Набор тестов не читает `DATABASE_URL`: это подключение приложения, и тест, который его возьмёт, поедет по данным разработчика.
    - Контейнер убирается за собой.
    
    ## Почему миграция здесь, а не в задаче про схему
    Иначе получается цикл: тест миграции требует обвязки, а DoD обвязки требует миграции. Минимальная первая миграция разрывает его и заодно проверяет самое хрупкое место в конфигурации — что поднятый образ действительно несёт pgvector нужного слота.
    
    ## Источник
    `docs/03-storage.md` § Задачи, `docs/09-build-and-deploy.md` § Задачи, `AGENTS.md` § Build & Test.
    
  comments: []
  linked_issues: []
  linked_prs: []
round_cap: 4
questions_per_round_cap: 3
round: 5
agent_id: a9e90b90a58970a4b
prior_qa:
  - round: 1
    question: |
      Входит ли в эту задачу прогон контейнерного теста в CI, или обвязка целится только в машину разработки? Текст задачи называет сокет машины разработки, а docs/09-build-and-deploy.md § Задачи про тесты конформности говорит: «идут в обычном прогоне — локально и в CI. Значит контейнерный рантайм нужен не только Postgres».
    answer: |
      И в CI
  - round: 1
    question: |
      Чем фиксируется версия образа? Тег pgvector/pgvector:pg18 плавающий — реестр перекладывает его на новую сборку (сейчас за ним 0.8.6, рядом лежат теги с версией: 0.8.6-pg18). Текст задачи называет плавающий тег и одновременно требует совпадения с версиями машины разработки.
    answer: |
      Как есть
  - round: 2
    question: |
      Спека готова: 8 пунктов объёма, 3 решения, 8 критериев приёмки, все гейты зелёные. Принять и поставить перекрёстную ссылку в issue #13?
    answer: |
      Снять AC5–AC7. Строгое чтение Rule 1: три критерия повторяют AGENTS.md § Build & Test — убрать их. Остальные ряды определяют готовность.
  - round: 3
    question: |
      Делегат снял не только AC5–AC7, но и пункты объёма 5–7 (те же три стандартных правила, записанные в другом разделе). Это совпадает с тем, что вы имели в виду?
    answer: |
      Да, принять. Чтение делегата верное: правило снимается из всех разделов сразу, а не только из таблицы критериев приёмки.
  - round: 4
    question: |
      docs/03-storage.md:7 предписывает Ryuk + OnceCell + #[sqlx::test]. Проверено: Ryuk в крейте нет, значение в static не дропается, sqlx::test берёт подключение только из DATABASE_URL. Дизайн предлагает свой main (harness=false + libtest-mimic). Как поступаем?
    answer: |
      Подмена + правка docs. Принять свой main и в этом же PR поправить docs/03-storage.md:7, чтобы корпус описывал то, что действительно работает. Стандартные правила (уборка контейнера, запрет на DATABASE_URL в тестах) остаются нетронуты.
  - round: 4
    question: |
      Ранеры GitHub тянут образ анонимно с общих адресов, где у Docker Hub есть лимит на вытягивание. Закладываться ли сейчас на обход лимита?
    answer: |
      Ничего сейчас. Не усложнять задачу; если CI упрётся в лимит — разбираться отдельной задачей по факту красного прогона.
  - round: 4
    question: |
      Задача частично закрывает пункты-чекбоксы в docs/. Отмечать ли их в этом PR?
    answer: |
      Отметить закрытое. Отметить те пункты, которые эта задача закрывает целиком, остальные оставить.
  - round: 5
    question: |
      Реализация разошлась с дизайном: дизайн измерял testcontainers 0.28.0, в коде разрешился 0.27.3 — потому что testcontainers-modules 0.15.0 (самая свежая) требует ^0.27.0. Код зелёный, make verify прошёл. Как закрываем расхождение?
    answer: |
      Правка + ревью. Штатный путь: design-writer приводит координаты D3/D4/D5 к 0.27.3, затем design-review прогоняется заново.
  - round: 6
    question: |
      self-review: APPROVE, но в коде есть утверждение, истинное по определению («ни одна миграция не ниже минимальной»), и его предписывает дизайн. Покрытие AC2 не страдает. Правка дизайна требует повторного design-review, а все четыре разрешённых раунда израсходованы. Как поступаем?
    answer: |
      Править без ревью. Дизайнер убирает предписание из § Test Design и подзадачи 1, исполнитель сносит мёртвую строку, повторный design-review для этой правки не запускается — точечное освобождение владельца.
```
