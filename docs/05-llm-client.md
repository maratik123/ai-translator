# 05. Клиент модели (llama-server)

## Выбор движка
Ходим в `llama-server` из llama.cpp напрямую. Ollama с мая 2026 (PR ollama#16031 «Remove CGO engines, use llama-server exclusively for GGML models») сама запускает `llama-server` как подпроцесс и пинит его версию файлом `LLAMA_CPP_VERSION`; собственный движок у неё остался только для MLX на Apple Silicon. То есть на нашей платформе Ollama — это менеджер моделей поверх того же бинарника, но без доступа к тому, что нам нужно:

- нет управления MoE-офлоадом (`--n-cpu-moe`), ollama#11772 открыт с августа 2025;
- открытые баги ровно на RDNA4: ollama#16261 (сегфолт при частичном офлоаде на RX 9070), ollama#16624 (регрессия ROCm на gfx1201), ollama#18117 (Vulkan не выделяет ~1 ГБ при >14 ГБ свободных), ollama#18129 (перезапуск раннера с дефолтным ctx 4096);
- нет прямого управления кэшем префикса и слотами, на которых построена схема промпта из `04-translation-pipeline.md`.

Совместимость с Ollama по OpenAI-совместимому API сохраняем как fallback в конфиге: тот же `/v1/chat/completions`, отличия — имя модели и отсутствие `/health` (вместо него `/api/tags`).

## Задачи
- [ ] `reqwest` клиент к `POST {base_url}/v1/chat/completions`, `stream: true`, парсинг SSE-чанков.
- [ ] Настройки: `base_url`, `model`, `temperature`, `top_p`, `top_k`, `min_p`, `repeat_penalty`, `max_tokens`, `timeout`, `api_key` (пусто для локального).
- [ ] Структурированный вывод для компактификации: `response_format: {type: json_schema}` (llama-server поддерживает), фолбэк на «только JSON» в промпте + `serde_json` с очисткой ```json.
- [ ] Health-check: `GET /health` раз в 10 с, статус в `LlmStatus`.
- [ ] Подсчет токенов из `usage` в ответе для метрик.
- [ ] Снятие метрик с `GET /metrics` (сервер запускается с `--metrics`), см. раздел «Метрики».
- [ ] Клиент эмбеддингов: `POST {embed_url}/v1/embeddings`, батчи по 32 абзаца, размерность из конфига. **Нормализация не нужна** — llama-server отдаёт уже нормализованные векторы (проверено: норма 1.0000).

## Параметры сэмплинга
Дефолты llama.cpp (`common/common.h`): `temp 0.80`, `top_k 40`, `top_p 0.95`, `min_p 0.05`, `repeat_penalty 1.00` (выключен). Осторожно: README llama-server в прозе утверждает, что `repeat_penalty` по умолчанию 1.1 — это устаревший текст, в коде 1.0.

Наши значения для перевода:

| Параметр | Значение | Почему |
|---|---|---|
| `temperature` | 0.2-0.3 | Воспроизводимость: иначе кэш переводов бесполезен для сравнения моделей и промптов |
| `top_p` | 0.9 | |
| `top_k` | 64 | |
| `min_p` | 0.05 | |
| `repeat_penalty` | **1.0, явно** | Штраф за повтор прицельно вреден: в художественном тексте обязаны повторяться имена персонажей, глаголы речи и термины глоссария. Мы сами кладём в хвост промпта предыдущие абзацы и RAG-пары с теми же именами, так что штраф работал бы против требования «следуй глоссарию» |

Значения передаются **в каждом запросе**, а не берутся из командной строки сервера: `top_k`, `min_p`, `repeat_penalty` — расширения llama.cpp к OpenAI-схеме, сервер их принимает в теле запроса.

Если карточка модели рекомендует свой профиль сэмплинга (как HauhauCS-сборка Gemma 4: `temp 0.6`, `repeat_penalty 1.1`), он, как правило, подобран под creative writing и roleplay, а не под перевод. Берём оттуда только `top_k`/`top_p`/`min_p`, температуру и штраф за повтор ставим свои. Расхождение проверяется через `eval`.

## Модели

### Перевод
Кандидаты под RX 9070 XT 16 ГБ + 32 ГБ DDR4:

| Модель | Тип | Q4 | Роль |
|---|---|---|---|
| **`ggml-org/gemma-4-26B-A4B-it` Q4_0** | MoE 26B, 4B активных, 128 экспертов, 8 активных; QAT | **14.6 ГБ** | **Основная** |
| HauhauCS QAT Uncensored Q4_K_M | то же + расцензуривание | 16.8 ГБ | Проверена и отклонена, см. ниже |
| Qwen3.6-35B-A3B | MoE 36B, 3B активных | 22.4 ГБ (UD-Q4_K_XL) | Второй участник сравнения |
| TranslateGemma 27B/12B | MT-специалист Google, 55 языков | | **Только baseline в `eval`**: контекст 2K токенов и модель не выполняет инструкции вне перевода — ни глоссария, ни JSON-компактификации |

Gemma 4 квантована с учётом квантования (QAT), поэтому Q4_K_M — рабочая точка, а не компромисс: более высокие кванты добавляют размер без выигрыша в качестве.

`mmproj` (зрение) не нужен: по `02-book-import.md` абзацы с картинками помечаются `kind = image` и на перевод не отправляются.

### Расцензуренные сборки
Для художественного текста safety-тюнинг даёт два режима отказа: громкий (модель отказывается — ловится `validate`) и **тихий (переводит, но смягчает)**. Второй опаснее, потому что проходит все текущие проверки: ответ не пустой, не эхо оригинала, число предложений сходится, служебных фраз нет. Поэтому расцензуренная сборка — обоснованный выбор, но не бесплатный:

- метод абляции обычно не раскрывается и умеет ронять следование инструкциям, а на нём держится глоссарий и JSON-компактификация;
- «0/465 Refusals» — метрика отказов, о качестве перевода не говорит ничего;
- если тюнинг делался на английском материале (`language: en` в метаданных), под FR/ES/IT/DE и русский выход он не проверялся;
- формулировка «reasons before answering» конфликтует с требованием «переводи только данный абзац, без пояснений» и повышает долю ретраев.

Решается замером против немодифицированного контроля, метрики — в `11-core-api-and-cli.md`.

**Итог проверки: расцензуренная сборка не нужна.** Прямое сравнение на полной книге (92 абзаца, повествование от первого лица с тяжёлым для safety-тюнинга содержанием, EN→RU, жадное декодирование, один seed, один промпт):

| | `ggml-org/gemma-4-26B-A4B-it` QAT Q4_0 | HauhauCS QAT Uncensored Q4_K_M |
|---|---|---|
| Размер | **14.6 ГБ** | 16.8 ГБ |
| Влезает в VRAM целиком | **да, под ROCm** | нет ни под одним бэкендом |
| **Отказов** | **0** | **0** |
| Отношение длин | 1.10 | 1.09 |
| Лексика регистра | 49 | 47 |
| Сниженная лексика, сильная степень | **2** | 1 |
| Эвфемизмы | 4 | 3 |
| Род 1 л., женский | 79 | 76 |
| Слова вне словаря, реальные ошибки | **0** | 2 |

Базовая модель не отказала ни разу, дала тот же регистр (и даже чуть больше лексики сильно сниженного слоя) и **меньше ошибок**. Плюс она на 2.2 ГБ меньше, а это решает: под ROCm она умещается в VRAM целиком, тогда как расцензуренной офлоад нужен при любом бэкенде (см. раздел «Бэкенд»).

Строки таблицы, кроме размера, получены однократными прогонами. Для качественных признаков (отказы, ошибки, род) это допустимо — они дискретны и не плавают от прогона к прогону. Сравнивать по ним время было бы нельзя, поэтому времени в таблице нет.

Значит «0/465 Refusals» измеряет проблему, которой для нашей задачи нет: базовая Gemma 4 переводит такой материал без сопротивления. **По умолчанию берём `ggml-org/gemma-4-26B-A4B-it` Q4_0.**

Существенная оговорка: системный промпт во всех прогонах содержал требование «не смягчай и не облагораживай, физический дискомфорт переводи как дискомфорт». Возможно, работу делает именно оно, и без него базовая модель смягчала бы сильнее. Это не проверено и стоит проверить — но на выводе не сказывается, потому что инструкция у нас есть в любом случае.

Оговорки о границах замера: один текст, один язык, один жанр; кванты разные (Q4_0 против Q4_K_M), хотя разница в пользу более крупного.

**Замерено** ранее на коротком фрагменте того же текста:

| Что проверяли | Результат |
|---|---|
| Отказ | нет, `finish_reason: stop`, маркеров отказа нет |
| Пропуски | нет, выравнивание 54/54 предложения |
| Род от первого лица | 6 из 6 верно |
| Прямая лексика | сохраняется |
| Регистр | **садится**: русский выход нейтрально-клинический, а не сниженный, при грубом оригинале |
| Физический дискомфорт | **смягчается**: `gag` → «перехватило дыхание» вместо «подступал рвотный позыв» |

Вывод: расцензуривание убрало **отказы**, но не сохранило **регистр**, и смягчение бьёт прицельно по описаниям дискомфорта, а не по сексуальной откровенности. Значит «0/465 Refusals» из карточки модели подтверждается и при этом действительно ничего не говорит о качестве.

Смягчение частично лечится промптом: точечная инструкция вида «слово X переводить как Y, а не как эвфемизм Z» исправляет конкретное место, а общее требование сохранять сниженный регистр базовый словарь почти не сдвигает. Практический вывод для `04-translation-pipeline.md`: регистр и спорные термины закрепляются в `style.register` и `terms` глоссария по конкретной книге, а не разовой фразой в системном промпте.

### Происхождение основной модели
Из `convert.log`, приложенного к репозиторию `ggml-org/gemma-4-26B-A4B-it-GGUF`, видно, как именно собраны наши файлы:

```
# сама модель — из QAT-весов, чистый Q4_0, эмбеддинги токенов оставлены в Q8_0
convert_hf_to_gguf.py <gemma-4-26B-A4B-it-qat-q4_0-unquantized> --outtype bf16 → …-QAT-BF16.gguf
llama-quantize --pure --tensor-type '^token_embd=q8_0' …-QAT-BF16.gguf → gemma-4-26B-A4B-it-Q4_0.gguf Q4_0

# MTP-голова — тоже из QAT
convert_hf_to_gguf.py <…-qat-q4_0> --outtype bf16 → mtp-…-QAT-BF16.gguf
llama-quantize --pure mtp-…-QAT-BF16.gguf → mtp-gemma-4-26B-A4B-it-Q4_0.gguf Q4_0
```

Отсюда два следствия:

- **`--pure` объясняет выбор Q4_0 вместо Q4_K_M.** Gemma 4 обучена с учётом квантования ровно в q4_0, поэтому смешанные K-кванты не улучшили бы качество, а только добавили размер. Единственное исключение — `token_embd` в Q8_0.
- **MTP-голова у нас QAT-происхождения**, а та, что идёт с расцензуренной сборкой, — от обычных assistant-весов. Файлы одинаковы по структуре (49 тензоров, 23 × Q4_0 + 26 × F32, длина данных байт в байт), но веса разные, и вот почему. На приём черновиков это, как показал замер, не влияет.

### Альтернативные драфтеры: проверены, ни один не подошёл
Сборка принимает `--spec-type` из списка `draft-simple`, `draft-eagle3`, `draft-mtp`, `draft-dflash`, `draft-dspark`, `ngram-simple`, `ngram-map-k`, `ngram-map-k4v`, `ngram-mod`, `ngram-cache` (через запятую комбинируются). Замер на той же книге, ROCm без офлоада:

| Режим | Время | Приём | VRAM |
|---|---|---|---|
| **MTP, голова Q4_0** | **70 с** | **52.8%** | 15.10 ГБ |
| MTP, голова Q8_0 | 76 / 75 с | 42.7% | 15.30 ГБ |
| `ngram-mod` | 75 / 75 с | — | 14.73 ГБ |
| `ngram-cache` | 76 с | 1.4% | 14.72 ГБ |
| `ngram-simple` | 75 с | — | 14.72 ГБ |
| `ngram-map-k` | 76 с | — | 14.72 ГБ |
| `draft-dflash` | не запускается | — | — |

Без спекулятивки вообще — 75 / 75 / 74 с, то есть все n-gram-режимы дают ровно ноль.

**Драфтер должен совпадать с целью по кванту, а не быть точнее её.** Голова Q8_0 даёт приём 42.7% против 52.8% у Q4_0 — более точный драфтер угадывает хуже. Логиты целевой модели несут собственную ошибку квантования Q4_0; голова, квантованная так же и из той же QAT-линии, эту ошибку разделяет, а более точная от неё расходится. Отсюда и то, что чужая голова расцензуренной сборки давала тот же приём — она тоже Q4_0. BF16-головы по этой логике были бы ещё хуже, и запас VRAM на них тратить незачем.

**N-gram бесполезен для перевода по устройству задачи.** Эти режимы угадывают продолжение, копируя куски из контекста, и работают там, где выход пересекается со входом: правка кода, суммаризация с цитированием, ответ по документу. У нас вход на одном языке, выход на другом — пересекаться нечему, отсюда приём 1.4%. Настройки `--spec-ngram-*` крутить бессмысленно.

#### Как устроен `ngram-cache` и почему его нельзя добавить к MTP
Разобрано по исходникам, чтобы не исследовать это заново.

`ngram-cache` — не модель, а хеш-таблица (`common/ngram-cache.h`): ключ это последовательность из 1-4 токенов (`LLAMA_NGRAM_MIN/MAX`), значение — счётчики того, какие токены за ней встречались. Черновик строится по трём таблицам сразу:

| Таблица | Откуда берётся | Флаг |
|---|---|---|
| `nc_context` | текущий контекст, строится на лету | — |
| `nc_dynamic` | накапливается по предыдущим генерациям, сохраняется на диск | `-lcd` |
| `nc_static` | построена по большому корпусу, используется для валидации | `-lcs` |

Отсюда и дешевизна: ни одного прохода через модель, ни весов, ни VRAM. Замер подтверждает — 14.72 ГБ против 15.10 ГБ с MTP, ровно на стоимость драфт-головы меньше. Платим только тем, что отвергнутые черновые токены занимают места в батче верификации.

**Комбинировать с MTP бесполезно.** `--spec-type` принимает список через запятую, но это **цепочка фолбэков по порядку**, а не объединение. Из `common/speculative.cpp`:

```c
for (auto & impl : spec->impls) {
    impl->draft(dparams);
    if (dp.drafting && !result.empty()) {
        dp.drafting = false;          // первый, кто выдал черновик, забирает шаг
        spec->impl_last[seq_id] = impl.get();
    }
    if (n_drafting == 0) break;
}
```

- `draft-mtp,ngram-cache` — MTP делает проход по модели и черновик выдаёт всегда (`p_min=0.00`, отказываться не от чего), поэтому ngram-cache не сработает ни разу;
- `ngram-cache,draft-mtp` — ngram получает приоритет и при каждом совпадении **вытесняет** черновик MTP, меняя догадки с приёмом 52.8% на догадки с приёмом 1.4%.

Порядок «дешёвый первым» был бы разумен, будь ngram точным. Он не точен.

**Оговорка.** Замер шёл с холодным `nc_dynamic`: кэш начинался пустым и накапливался 92 абзаца. Флаг `-lcd` сохраняет его между запусками, так что на целой книге или библиотеке он был бы полнее. Потолок всё равно низкий по причине выше. Собрать `nc_static` по корпусу нечем: `llama-lookup-create` в нашей сборке не установлен.

**DFlash не помещается.** `dflash-gemma-4-26B-A4B-it-Q8_0.gguf` — 472 МБ, но он драфтит блоками по 16 токенов (`block_size=16` против `n_max=2` у MTP), и вычислительные буферы под это в VRAM не влезают: `ROCm error: out of memory` и без офлоада, и с вынесенным блоком экспертов. На карте 16 ГБ этот режим недоступен.

### Эмбеддинги
**По умолчанию `bge-m3` Q8_0** (`ggml-org/bge-m3-Q8_0-GGUF`, 635 МБ), кандидат на замену —
`jina-embeddings-v5-text-small-text-matching` (разбор ниже). Считаем на CPU: вся книга примерно
за 6 с при нулевой VRAM.

```
llama-server -m models/ggml-org/bge-m3-Q8_0-GGUF/bge-m3-q8_0.gguf \
  --embedding -ub 2048 -c 2048 -np 1 \
  -dev none -ngl 0 -t 8 --host 127.0.0.1 --port 8081
```

Харнесс замера — `tools/emb-eval/emb_eval.py`, сырые ранги прогона — `tools/emb-eval/results-2026-09-17.json`.

#### Префикс входа решает больше, чем выбор модели
Каждая модель ждёт свою форму входа, она лежит в её `config_sentence_transformers.json`.
Наша задача симметричная (абзац ищет абзац), поэтому префикс ставится на **обе** стороны.

| модель | префикс для нашей задачи | что даёт |
|---|---|---|
| `jina-v5-*-text-matching` | `Document: ` | кросс-язык R@1 54.3 → **97.8**, RAG R@3 62.3 → **72.5** |
| EmbeddingGemma | `task: sentence similarity \| query: ` | RAG R@3 56.5 → **63.8** против документного `title: none \| text: ` |
| bge-m3, granite, harrier | нет | в конфиге пусто |
| arctic, Qwen3 | нет | префикс объявлен только для стороны запроса в асимметричном поиске |

Без префикса `jina-v5` выглядит сломанной, и по первому прогону я едва не отбраковал лучшую модель.
Это **не противоречит** правилу «инструкции ухудшают» ниже: инструкция вида `Instruct: …\nQuery:`
на одной стороне уводит запрос в другую область пространства, а фиксированный префикс формы,
поставленный симметрично, возвращает модель в тот режим, в котором её учили.

#### Замер на настоящей задаче (RAG по книге)
Эталон строится без ручной разметки: золотым соседом считается **более ранний** абзац, делящий
с запросом редкое слово книги (df 2-6, без стоп-слов), база ограничена `p.id < i` как в SQL из
`03-storage.md`. 92 абзаца, 69 запросов, случайный уровень R@1 12.3%, R@3 30.5%.

| модель | мс/абзац | RAG R@1 | RAG R@3 | RAG MRR | сосед R@3 | кросс R@1 | d′ |
|---|---|---|---|---|---|---|---|
| **jina-v5-small-tm** | 86 | 46.4 | **72.5** | **61.0** | 17.3 | 97.8 | 1.29 |
| jina-v5-nano-tm | **19** | **47.8** | 66.7 | 59.7 | 16.0 | 97.8 | 1.21 |
| Qwen3-Embedding-0.6B | 87 | 43.5 | 65.2 | 56.7 | 12.3 | 95.7 | 1.11 |
| EmbeddingGemma-300M (STS-префикс) | 26 | **47.8** | 63.8 | 58.8 | 13.6 | 97.8 | **1.32** |
| granite-311m-r2 | 21 | 43.5 | 60.9 | 54.7 | **21.0** | 95.7 | 1.03 |
| **bge-m3** | 55 | 42.0 | 59.4 | 54.5 | 16.0 | 98.9 | 0.90 |
| arctic-embed-l-v2.0 | 54 | 42.0 | 56.5 | 53.1 | 16.0 | 96.7 | 0.41 |
| harrier-oss-v1-0.6b | 88 | 37.7 | 55.1 | 50.7 | 13.6 | **100.0** | 0.86 |

«сосед» — попал ли непосредственно предыдущий абзац в top-3 (случайный уровень 8.0%);
d′ — отрыв золотых пар от остальных в единицах разброса остальных.

**Значимость** (парный бутстрап 20k ресэмплов и знаковый тест по рангам, всё против bge-m3):
- `jina-v5-small`: ΔR@3 **+13.0 пп [+2.9, +23.2]**, ΔMRR +6.5 [0.0, +13.2] — интервал не накрывает ноль;
- `jina-v5-nano`: лучше в 29 запросах, хуже в 10, p = 0.003 — **единственный результат, переживающий
  поправку на 8 сравнений** (0.027);
- EmbeddingGemma со STS-префиксом: 27 против 11, p = 0.014, после поправки 0.11 — тенденция, не доказательство;
- granite, arctic, harrier, Qwen3 от bge-m3 неотличимы.

Время на абзац между прогонами гуляет на десятки процентов (Qwen3 давал и 141 мс, и 87) — эмбеддинги
считает фоновый воркер, и решающим этот столбец не является.

#### Порог отсечки: у каждой модели свой
Медиана близости по всем парам абзацев книги — она же ориентир для порога из `04-translation-pipeline.md`:

| модель | медиана | p95 | max |
|---|---|---|---|
| granite-311m-r2 | 0.834 | 0.894 | 0.948 |
| jina-v5-nano-tm | 0.611 | 0.778 | 0.915 |
| jina-v5-small-tm | 0.598 | 0.776 | 0.901 |
| harrier-oss-v1-0.6b | 0.573 | 0.737 | 0.862 |
| EmbeddingGemma (STS) | 0.499 | 0.692 | 0.835 |
| bge-m3 | 0.496 | 0.655 | 0.842 |
| arctic-embed-l-v2.0 | 0.476 | 0.661 | 0.860 |
| Qwen3-Embedding-0.6B | 0.356 | 0.568 | 0.767 |

Порог зависит и от префикса: у `jina-v5-small` медиана 0.486 без `Document: ` и 0.598 с ним.
У granite пространство схлопнуто, отсечка «меньше 0.5» там не отсечёт ничего.

#### Отбор кандидатов: MTEB(Multilingual, v2)
Из 131 задачи бенчмарка нам соответствует по форме только STS (симметричное «текст ↔ текст»),
поэтому средний балл и колонка retrieval не смотрятся вовсе. Числа посчитаны из результатов
`embeddings-benchmark/results` по всем 15 STS-задачам бенчмарка; «наши языки» — среднее по
одноязычным подмножествам en/fr/es/it/de (16 штук), кросс-языковые пары выброшены.

| модель | STS, все 15 | STS, наши языки |
|---|---|---|
| jina-v5-text-small | 78.3 | **84.0** |
| jina-v5-text-nano | 77.5 | 83.9 |
| Qwen3-Embedding-0.6B | 75.4 | 83.3 |
| harrier-oss-v1-0.6b | 76.4 | 82.5 |
| EmbeddingGemma-300M | 73.8 | 81.3 |
| bge-m3 | 73.6 | 79.2 |
| granite-311m-r2 | 67.9 | 76.8 |
| arctic-embed-l-v2.0 | 69.1 | 76.8 |

**Рейтинг не воспроизвёлся.** harrier — первый по MMTEB v2 (69.0 по карточке) и четвёртый по STS —
у нас последний по RAG. arctic и granite были последними по STS и оказались в середине. Совпала
только `jina-v5`. Лидерборд годится как фильтр кандидатов и не годится как решение.

Жёсткие фильтры при отборе оказались важнее баллов: контекст ≥ 2048 токенов (самый длинный абзац
книги — 902, весь слой моделей на 512 отпадает), размерность ≤ 2000 (потолок HNSW в pgvector для
`vector`), и арка должна грузиться нашей сборкой. Список поддерживаемых арок берётся из самой
библиотеки: `strings "$(ldd $(command -v llama-server) | awk '/libllama/{print $3}')" | grep -E "bert|embed"`. На b10927 это
`bert`/XLM-R, `jina-bert-v2/v3`, `modern-bert`, `neo-bert`, `nomic-bert(-moe)`, `gemma-embedding`,
`qwen3`, `eurobert`, `granite-embed-multi-97m/311m`, `llama-embed`, `t5encoder`; арки `gte`/`NewModel`
нет, поэтому `gte-multilingual-base` и `arctic-embed-m-v2.0` отпадают до всякого замера.

#### Первый замер (кросс-языковой прокси)
Историческая таблица, три модели, задача «найти английский исходник русского абзаца среди 92»:

| Модель | dim | Контекст | мс/абзац | Кросс-язык R@1 | MRR |
|---|---|---|---|---|---|
| **bge-m3 Q8_0** | 1024 | 8192 | 63 | **98.9** | **99.5** |
| EmbeddingGemma-300M Q8_0 | 768 | 2048 | **30** | 96.7 | 98.0 |
| Qwen3-Embedding-0.6B Q8_0 | 1024 | 32768 | 141 | 95.7 | 97.6 |

Вторая задача того замера — найти тот же абзац среди 1012 абзацев из 11 других переводов — дала
ровно 100% у всех трёх и модели не различает: переводы не независимы, половина сделана одной
моделью с одними настройками и различается только шумом сэмплирования.

Две поправки к этому замеру, обе всплыли позже: **EmbeddingGemma там мерена без префикса**, то есть
занижена, и сам кросс-языковой прокси нашу задачу предсказывает плохо — `harrier` берёт по нему
100.0 и при этом последний на RAG.

Проверено дополнительно:
- **f16 не нужен.** `Qwen3-Embedding-0.6B` в f16 дала ровно те же 95.7 R@1, что и Q8_0; `jina-v5-nano`
  в f16 дала ровно те же метрики, что Q8_0 (R@3 66.7, кросс 97.8, медиана 0.611). Правило
  подтверждено и на encoder-, и на decoder-only архитектуре; файлы f16 удалены.
- **Инструкции ухудшают.** У Qwen3 instruction-aware-режим на стороне запроса уронил R@1 с 95.7 до 91.3.
  Механизм рассчитан на асимметричный поиск «короткий запрос → длинный документ», а у нас абзац ищет
  абзац. Про отличие от обязательного префикса формы — выше.
- **Пулинг не задавать вручную.** Все GGUF объявляют его в метаданных (`bert`→cls, `gemma-embedding`→mean,
  `modern-bert`→cls, `qwen3`/`eurobert`→last), llama.cpp читает оттуда. Флаг `--pooling` нужен только
  для старых сборок GGUF без этого поля.
- **`-ub` должен вмещать самый длинный отдельный абзац**, а не батч. При `-ub 512` сервер отвечает
  `input (902 tokens) is too large to process` на длинном русском абзаце. Ставим `-ub 2048`.

#### Что лежит на диске
`bge-m3` (действующая), `EmbeddingGemma-300M`, `jina-v5-text-small-text-matching`,
`jina-v5-text-nano-text-matching`. Проверены и удалены как ничего не добавляющие:
`Qwen3-Embedding-0.6B`, `granite-embedding-311m-multilingual-r2`, `snowflake-arctic-embed-l-v2.0`,
`harrier-oss-v1-0.6b` — строки для них оставлены в конфиге харнесса, чтобы прогон можно было повторить.

**Смену модели по умолчанию откладываю до второй книги.** 69 запросов с одного текста — мало,
чтобы переиндексировать базу, а у `jina-v5` вдобавок лицензия CC-BY-NC-4.0 против MIT у bge-m3.
Размерность при замене не меняется (1024), но порог в `04-translation-pipeline.md` придётся
переписать с 0.5 на 0.6. Что стоит сделать независимо от выбора модели — научить клиент
эмбеддингов ставить префикс из конфига модели.


## Бэкенд: решает не скорость, а вместимость

**Замерено на полном тексте, оба бэкенда × обе модели × MTP вкл/выкл.** Раскладка подбиралась `llama-fit-params` отдельно под каждый бэкенд.

| Модель | Бэкенд | Блоков на CPU | без MTP | с MTP |
|---|---|---|---|---|
| **`ggml-org` Q4_0** | **ROCm** | **0** | 75 / 75 / 74 с | **66 / 65 / 65 с** |
| `ggml-org` Q4_0 | Vulkan | 3 | 87 / 105 / 107 с | 89 с |
| HauhauCS Q4_K_M | Vulkan | 6 | 139 с | 118 с |
| HauhauCS Q4_K_M | ROCm | 5 | 149 с | 112 с |

Строки с несколькими числами — повторные измерения.

**Ключевое отличие бэкендов здесь не в скорости арифметики, а в доступной памяти.** `--list-devices` показывает `ROCm0: 16230 MiB free` против `Vulkan0: 15148 MiB free`. Этого гигабайта хватает, чтобы 14.6-гигабайтная модель уместилась под ROCm **целиком**, и офлоад на CPU исчез. На большой модели запаса не хватает, офлоад нужен обоим, и преимущество ROCm пропадает.

**Воспроизводимость важнее средних.** Конфигурация без офлоада даёт разброс ±1 с. Конфигурация с офлоадом на той же машине плавает на 20% (87 → 105 → 107 с при идентичных аргументах). Причина: с `--load-mode none` веса читаются в RAM целиком, а вынесенные эксперты живут в host-памяти; при 31 ГБ RAM и 32 ГБ моделей на диске часть страниц уходит в swap (`pswpout` растёт), и каждый токен, которому нужен вынесенный эксперт, идёт через диск. Скорость начинает зависеть от того, что ещё успело осесть в памяти.

Это отдельно бьёт по ночному полному переводу из `04-translation-pipeline.md` — длинному фоновому прогону, где непредсказуемость хуже, чем несколько процентов скорости.

**Правило выбора:** прогнать `llama-fit-params` под оба бэкенда. Если под одним офлоад обнуляется, а под другим нет — брать первый. Если офлоад нужен обоим, разница между бэкендами на нашем замере оказалась в пределах систематики, так что выбирать можно по любому другому соображению.

**Осторожно с методикой.** Первая версия этого замера прогнала 8 конфигураций подряд по одному разу и получила порядок-зависимую систематику: каждая следующая загрузка модели ухудшала состояние памяти, так что ранние конфигурации выглядели быстрее поздних. Различия меньше 20% в такой схеме неразличимы. Строки без повторов в таблице выше сохранены как есть и интерпретации не подлежат.

### Ловушка с выбором устройства
`-dev` обязателен, но не из-за lavapipe: llama.cpp сама отфильтровывает софтверные Vulkan-устройства. Причина в другом — при сборке с обоими бэкендами `--list-devices` показывает **одну и ту же карту дважды**:
```
ROCm0:   AMD Radeon RX 9070 XT (16304 MiB, 16230 MiB free)
Vulkan0: AMD Radeon RX 9070 XT (RADV GFX1201) (16304 MiB, 15148 MiB free)
```
Без `-dev` llama.cpp разложит модель по «двум» устройствам, которые физически один GPU, и выделит память дважды. Всегда прибиваем: `-dev Vulkan0` или `-dev ROCm0`.

Проверено на этой машине: RADV на gfx1201 отдаёт только `VK_KHR_cooperative_matrix` (coopmat1); `VK_NV_cooperative_matrix2` есть исключительно у lavapipe. Оптимизации llama.cpp, идущие по пути coopmat2, нашу карту не затрагивают.

## Запуск llama-server (deploy/)

### Подбор раскладки
Не подбирать `--n-cpu-moe` вручную — есть `llama-fit-params`, который считает раскладку под доступную память:
```
llama-fit-params -m models/<model>.gguf -dev ROCm0 -c 16384 -ctk q8_0 -ctv q8_0 -fa on -t 8
```
Он печатает готовую строку аргументов, и это точнее `--n-cpu-moe N`, потому что выносит конкретные тензоры, а не «первые N слоёв». Прогонять под **оба** бэкенда: они видят разный объём свободной VRAM, и раскладка выходит разной.

Загрузчик при `-ot ...=CPU` предупреждает: `tensor overrides to CPU are used with mmap enabled - consider using --load-mode none for better performance`. Добавляем `--load-mode none`.

### Перевод (целевая конфигурация)
```
llama-server \
  -m models/ggml-org/gemma-4-26B-A4B-it-GGUF/gemma-4-26B-A4B-it-Q4_0.gguf \
  -md models/ggml-org/gemma-4-26B-A4B-it-GGUF/mtp-gemma-4-26B-A4B-it-Q4_0.gguf \
  --spec-type draft-mtp --spec-draft-n-max 2 \
  -dev ROCm0 -c 16384 -ngl 99 \
  -ctk q8_0 -ctv q8_0 -fa on -t 8 -np 1 --load-mode none \
  --metrics --host 127.0.0.1 --port 8080
```
- `-ot` не нужен: под ROCm модель влезает в VRAM целиком (14.34 ГБ, с MTP 14.71 ГБ сверх рабочего стола).
- `-t 8` по числу физических ядер, не 16: SMT на memory-bound нагрузке не помогает.
- 65 с на книгу из 92 абзацев, разброс ±1 с.

## MTP (спекулятивное декодирование)
Поддерживается для Gemma 4 и Qwen3.5/3.6. Флаги (сверено с `common/arg.cpp`):

- `-md, --model-draft, --spec-draft-model FNAME` — файл MTP-головы;
- `--spec-type draft-mtp` — тип спекулятивки (список через запятую);
- `--spec-draft-n-max N` / `--spec-draft-n-min N` — сколько токенов драфтить; начинать с 2, перебрать 1-6;
- `--spec-draft-cpu-moe`, `--spec-draft-n-cpu-moe` — офлоад драфтера, нам не нужен.

**Цена по памяти — 382 МБ** (замер: 14.34 ГБ без MTP против 14.71 ГБ с ним, сверх рабочего стола). Из них 241 МБ сам файл головы, остальное — её KV-кэш и вычислительные буферы. Расширять офлоад ради MTP не требуется.

### Что даёт MTP
Измерено с повторами на целевой конфигурации (`ggml-org` Q4_0, ROCm, офлоада нет): **75 / 75 / 74 с без MTP против 66 / 65 / 65 с с ним — 1.14x**. Полная таблица по бэкендам и моделям в разделе «Бэкенд» выше.

Заявленные Unsloth 1.4-2.2x и заявленные автором расцензуренной сборки ~35% на нашей нагрузке не достигаются. Причина не в MTP, а в форме нагрузки: префилл обрабатывает 24-37 тыс. токенов против ~5 тыс. сгенерированных, отношение **≈ 5 к 1**. Спекулятивка ускоряет только генерацию. **Увеличение доли кэша префикса даст больше, чем спекулятивка.**

Приём черновиков стабильно **48-49%** во всех замеренных сочетаниях модели и бэкенда: позиция 0 даёт 60-65%, позиция 1 — 36-43%. Падение вдвое ко второй позиции означает, что `--spec-draft-n-max 2` подобран верно и перебирать 1-6 незачем.

Приём не зависит от того, взята ли MTP-голова из той же сборки, что и модель: у расцензуренной сборки голова чужая (от немодифицированной модели), и приём у неё такой же. Значит ~50% — рабочая точка механизма, а не следствие рассогласования. Правила «брать голову только из своей сборки» не вводим, оснований нет.

### Вывод с MTP НЕ идентичен
Обещание «identical output» **не подтвердилось**. Замер: жадное декодирование (`temperature 0`, `top_k 1`), одна и та же раскладка, с MTP и без — совпало 5 из 12 абзацев, сходство 76%. Расхождения содержательные, а не пробельные: «Работаю я в известном…» против «Я работаю в известном…».

Алгоритм спекулятивки тут ни при чём: верификация действительно сохраняет распределение. Дело в арифметике — проверка N токенов за один проход меняет форму батча, а с ней порядок накопления чисел с плавающей точкой; логиты микроскопически плывут, и при близких вероятностях argmax выбирает другой токен.

То же самое объясняет более общий факт: **llama-server недетерминирован при сэмплировании даже с фиксированным seed** (совпало 8/12 абзацев, сходство 88%), потому что кэш префикса меняет разбивку на батчи. На жадном декодировании в пределах одной конфигурации сервер детерминирован — два прогона дали 12/12.

Практические следствия:
- `eval` и `compare` (`11-core-api-and-cli.md`) обязаны идти на `temperature 0`, **с выключенным MTP** и по возможности **без офлоада на CPU**, иначе шумовой пол перекроет измеряемый эффект;
- перевод в продакшене — не чистая функция от (модель, промпт, текст); `cache_key` из `03-storage.md` идентифицирует запись кэша, но не воспроизводимый результат.

## Режим рассуждений: выключать для перевода
Gemma 4 по умолчанию рассуждает перед ответом. На переводе это не «немного медленнее», а полный отказ: модель уходит в `message.reasoning_content`, `message.content` остаётся **пустым**, ответ упирается в `max_tokens` с `finish_reason: length`. Замерено: 400 токенов рассуждений о том, какой род выбрать, и ноль текста перевода.

Управление:

| Способ | Где | Работает |
|---|---|---|
| `chat_template_kwargs: {"enable_thinking": false}` | тело запроса | **да** — то, что нужно нам |
| `--reasoning off` (`-rea`) | аргумент сервера | да, но глобально |
| `reasoning_budget` | тело запроса | нет, только аргумент сервера |

Ставим **в каждом запросе** через `chat_template_kwargs`, потому что режим нужен разный: перевод абзаца — без рассуждений, компактификация контекста — возможно с ними (задача реже и тяжелее, стоит замерить обе ветки).

## Метрики
Сервер с `--metrics` отдаёт Prometheus-совместимый `GET /metrics`. Снимаем:

- `llamacpp:spec_decode_num_accepted_tokens_total` — принятые черновые токены;
- `llamacpp:spec_decode_num_accepted_tokens_per_pos_total` (метка `position="N"`) — на какой позиции черновик разваливается; заодно подсказывает оптимальный `--spec-draft-n-max`;
- ток/с и размер очереди — из `usage` ответов и из `/slots`.

`GET /props` даёт имя модели и настройки генерации по умолчанию, `GET /slots` — состояние слотов.

## Тестирование клиента

Три слоя, и они не заменяют друг друга: каждый ловит класс, слепой для остальных.

**1. Юнит-тесты на записанных фикстурах.** Разбор SSE-чанков, `usage`, пустой `content` при непустом `reasoning_content`, обрыв потока. Фикстура называет модель и параметры, её породившие. Записанная фикстура — **не мок**: она содержит то, что сервер реально прислал.

**2. Стаб HTTP-сервера.** Проверяет, что́ отправляем мы: `enable_thinking: false`, `repeat_penalty: 1.0`, полный набор полей; и как держим `500`, таймаут, оборванный поток. Детерминирован, модель не нужна.

**3. Конформность против настоящего `llama-server`** в контейнере, с мини-моделями из `testdata/models/`. Идёт и локально, и в CI — модели лежат в репозитории, образ тянется.

Третий слой нужен потому, что `chat_template_kwargs`, `repeat_penalty`, `min_p` и `top_k` — расширения llama.cpp к OpenAI-схеме, а «сервер их принимает» есть утверждение про конкретную сборку. Мок по построению кодирует наше убеждение и зелен ровно тогда, когда мы ошибаемся; фикстура честнее, но апгрейд llama.cpp её не трогает.

### Граница третьего слоя: «принял» не равно «применил»

Если сборка молча игнорирует неизвестное поле, мы получаем `200 OK` и зелёный тест при мёртвой фиче. Различить можно только по наблюдаемой разнице в выводе, а её даёт не всякая модель.

Практическое следствие: **что `enable_thinking: false` подавляет рассуждения, на мини-модели проверить нельзя** — режима рассуждений у неё нет. Эта проверка просто **вне тестового набора**, и тест конформности не должен выдавать себя за её замену.

### Тесты идут только на CPU и только на мини-моделях

Набор тестов **никогда** не обращается к серверу, обслуживающему приложение, и **никогда** не занимает GPU. Причина ресурсная: основная модель держит почти всю VRAM, а на машине 31 ГБ RAM при 32 ГБ моделей. Тест, который встанет в очередь за теми же ресурсами, делает недостоверными и себя, и работающий сервер: замер поплывёт у обоих, и ни один результат не будет означать того, что написано на нём.

Отсюда правило на случай нехватки: **если нужную проверку на мини-моделях сделать нельзя — это не повод брать реальную модель. Спросить владельца.** Варианты, которые он может выбрать, разные (другая мини-модель, отказ от проверки, ручной прогон вне набора), и выбор за ним, а не за тем, кто пишет тест.

Что мини-модель при этом доказывает полноценно: тело запроса принято без `400`; SSE-обрамление разбирается нашим кодом; формы `/health`, `/props`, `/slots`, `/metrics` те, что мы разбираем; `response_format: {type: json_schema}` действительно ограничивает вывод — это constrained decoding на уровне сэмплера, от качества модели оно не зависит, и на мелкой модели выходит бессмыслица, но схемно-валидная.

## Проверки перед разработкой
Сделано (числа в разделах выше): выбор модели, выбор бэкенда, раскладка, MTP, кэш префикса, эмбеддинги.

- [ ] Замерить качество на кандидатах (`ggml-org` Gemma 4 26B-A4B QAT, Qwen3.6-35B-A3B) вслепую на одной главе.
- [ ] Замерить компактификацию отдельно: у неё длинный вход и короткий JSON на выходе, то есть профиль нагрузки обратный переводу.
- [ ] Проверить, что память работает в двухканале и на XMP-профиле (`dmidecode -t memory`).
